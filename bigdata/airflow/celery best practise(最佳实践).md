1. 使用更多的Queue，不只用默认的Queue
2. 使用具有优先级的队列及Workers，在优先级不同的队列中，配置不同数量的Worker； 
   为不同任务指定不同的队列，从而达到不同的处理速率
3. 使用错误重试机制
4. 使用Flower UI界面
5. 不用太关注任务的退出状态，任务本身是异步，而应更关注任务的执行结果，注意任务状态≠执行结果
6. 不要传递数据库/ORM对象给任务，序列化或存在问题，状态过期等问题，一般是传递不变的ID，
   任务在数据库中实时获取其他属性